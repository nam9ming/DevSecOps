import React from 'react';

const Pipelines = () => (
  <div>
    <h1 className="text-2xl font-bold">Pipelines Page</h1>
    <p>Content for pipelines page goes here.</p>
  </div>
);

export default Pipelines;